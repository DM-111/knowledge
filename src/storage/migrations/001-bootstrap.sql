-- Bootstrap migration intentionally keeps the schema minimal.
-- Future stories must introduce business tables through new numbered migrations.
-- Do not add chunks, tags, item_tags, or chunks_fts here.

SELECT 1;
